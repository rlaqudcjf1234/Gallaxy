<?xml version="1.0" encoding="UTF-8"?>
<!-- mapper DTD 선언 -->
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="board">
	<insert id="insertBoard" parameterType="dto.BoardDTO">
		<selectKey resultType="String" keyProperty="boardId" order="BEFORE">
			SELECT MAX(IFNULL(BOARD_ID))+1 FROM BOARD
		</selectKey>
		INSERT INTO BOARD (
			BOARD_ID, BOARD_TITLE, BOARD_WORD_DATE, BOARD_WORD_TIME, BOARD_CONTENT, BOARD_FILE_PATH, USER_ID
		) VALUES (
			#{boardId}, #{boardTitle}, #{boardContent}, #{boardWordDate}, #{boardWordTime}, #{boardFilePath}, #{userId}
		)
	</insert>
</mapper>